apiVersion: trident.netapp.io/v1
kind: TridentBackendConfig
metadata:
  name: netapp-nas
  namespace: trident
spec:
  version: 1
  backendName: netapp-nas
  storageDriverName: ontap-nas
  managementLIF: 
  dataLIF: 
  svm: 
  spaceReserve: none
  defaults:
    snapshotReserve: "30"
    exportPolicy: 
  credentials:
    name: svm-cred
  labels:
    svm: 
